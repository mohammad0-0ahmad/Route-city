package testPackage;

class NetworkTest {

/* Commented because function modifier is private.
    @Test
    void getNodeSymbolTest() {
        Network network = Network.getInstance();
        assertEquals("G1",network.getNodeSymbol(6));
    }
*/

/*
 * Commented because function modifier is private.
    @Test
    void getRandomWeightTest(){
        //
        int min = 9, max=10;
        int random = Network.getRandomWeight(min,max);
        System.out.println(random);
        assertTrue((random >= min));
        assertTrue((random <= max));
        //
        min = 1; max=10;
        random = Network.getRandomWeight(min,max);
        System.out.println(random);
        assertTrue((random >= min));
        assertTrue((random <= max));
        //
        min = -1; max=1;
        random = Network.getRandomWeight(min,max);
        System.out.println(random);
        assertTrue((random >= min));
        assertTrue((random <= max));
        //
        min = 1; max=-1;
        random = Network.getRandomWeight(min,max);
        System.out.println(random);
        assertTrue((random >= -1 ));
        assertTrue((random <= 1));
        //
        min = -10; max = 0;
        random = Network.getRandomWeight(min,max);
        System.out.println(random);
        assertEquals(1,random);

    }
*/
}